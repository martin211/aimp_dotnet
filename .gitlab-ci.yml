variables:
  VERSION: "$AIMP_VERSION.$CI_JOB_ID"
  AimpDotnetProj: "$CI_PROJECT_DIR/Build/AimpDotnet.proj"

stages:
  - analysis_start
  - build
  - analysis_end
  - artifacts
  - zip_publish
  - documentation
  - publish_documentation
  - deploy

docker_start:
  stage: analysis_start
  except:
    - master
  script:
  - Import-Module CiBuildTools
  - Start-DockerContainer -containerName "dev.mysql"
  - Start-DockerContainer -containerName "dev.sonarqube"

analysis_start:
  stage: analysis_start
  except:
    - master
  script:
  - Import-Module CiBuildTools
  - Invoke-StartSonarQube -solutionPath $env:CI_PROJECT_DIR\src\aimp_dotnet.sln -sonarProjectKey $env:SONAR_PROJECT_KEY -sonarProjectName aimp_dotnet -version $env:VERSION -branch $env:CI_COMMIT_REF_NAME

analysis_end:
  stage: analysis_end
  except:
    - master
  script:
  - Import-Module CiBuildTools
  - Invoke-StopSonarQube

set_version:
  stage: build
  script:
  - Import-Module CiBuildTools
  - Invoke-SetCliVersion -rcFilePath "$env:CI_PROJECT_DIR\src\aimp_dotnet\aimp_dotnet.rc" -contentToReplace "1.0.0.1" -replaceTo "$env:VERSION"
  - Invoke-SetCliVersion -rcFilePath "$env:CI_PROJECT_DIR\src\aimp_dotnet\aimp_dotnet.rc" -contentToReplace "1,0,0,1" -replaceTo $env:VERSION.Replace(".", ",")

build_sdk_master:
  only:
    - master
  stage: build
  script:
  - powershell -File $env:CI_PROJECT_DIR/Build/build.ps1 -projFile $env:AimpDotnetProj -target Sdk -version $env:VERSION -configuration Release -projectDir $env:CI_PROJECT_DIR/Build/

build_plugins_master:
  only:
    - master
  stage: build
  script:
  - powershell -File $env:CI_PROJECT_DIR/Build/build.ps1 -projFile $env:AimpDotnetProj -target Plugins -version $env:VERSION -configuration Release -projectDir $env:CI_PROJECT_DIR/Build/

build_sdk_develop:
  only:
    - develop
  stage: build
  script:
  - $env:CI_PROJECT_DIR/Build/build.ps1 -projFile $env:AimpDotnetProj -target Sdk -version $env:VERSION -configuration Debug -projectDir $env:CI_PROJECT_DIR/Build/

build_plugins_develop:
  only:
    - develop
  stage: build
  script:
  - $env:CI_PROJECT_DIR/Build/build.ps1 -projFile $env:AimpDotnetProj -target Plugins -version $env:VERSION -configuration Debug -projectDir $env:CI_PROJECT_DIR/Build/

prepare_artifacts:
  only:
    - master
    - develop
  except:
    - tags
  stage: artifacts
  script:
  - Import-Module CiBuildTools
  - Invoke-BuildProject -Path $env:AimpDotnetProj -target CopyArtifacts -projectParameters "/p:Version=$VERSION /p:Configuration=Release /p:DeploymentFolder=artifacts" -BuildLogDirectoryPath $CI_PROJECT_DIR/Build/logs -KeepBuildLogOnSuccessfulBuilds -ShowBuildOutputInCurrentWindow

compress_artifacts:
  only:
    - master
    - develop
  except:
    - tags
  stage: artifacts
  script:
  - Import-Module CiBuildTools
  - Invoke-BuildProject -Path $env:AimpDotnetProj -target MakeZip -projectParameters "/p:Version=$VERSION /p:Configuration=Release /p:DeploymentFolder=artifacts" -BuildLogDirectoryPath $CI_PROJECT_DIR/Build/logs -KeepBuildLogOnSuccessfulBuilds -ShowBuildOutputInCurrentWindow
  artifacts:
    paths:
      - $env:CI_PROJECT_DIR/Build/artifacts/AIMP.SDK.zip
      - $env:CI_PROJECT_DIR/Build/logs/*

documentation_build:
  only:
    - master
  stage: documentation
  script: 
  - Import-Module CiBuildTools
  - Invoke-BuildDocumentation

documentation_publish:
  only:
    - master
  stage: publish_documentation
  script: 
  - Import-Module CiBuildTools
  - Invoke-BuildDocumentation

publish_to_release:
  stage: deploy
  only:
    - master
  environment:
    name: Production
  script:
  - Import-Module CiBuildTools
  - New-GitTag -tagName $env:VERSION